"""
Program that reads a radius of a circle, calculates and prints its area:
"""
import math
radius = float(input("Type the value of the radius:" ))
print("The area of the circle is equivalent to: ", math.pi * math.pow(radius,2))